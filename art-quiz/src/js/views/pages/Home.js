import data from "../../images.js";

export const Home = {
  render: async () => {
    // console.log(data);
    // const questionsByAuthor = [];
    // const questionsByPicture = [];

    return /*html*/ `
            <section class="section">
                <h1> Home </h1>
            </section>
        `;
  },
  after_render: async () => {},
};
